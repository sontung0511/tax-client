"use client";

import { formatVnd, parseVnd, type BusinessProfile, type CashPayment, type CashReceipt, type TaxBreakdown, type TaxDatabase, type TaxDeclaration, type Transaction } from "@/domain/tax";
import { taxApi } from "@/services/tax-api";
import { calculateTax } from "@/tax-engine/calculate";
import { validateTransactions } from "@/services/validate-transactions";
import { FormEvent, useEffect, useMemo, useRef, useState } from "react";
import type { View } from "./sidebar";

const statusLabel = { draft: "Đang nhập", review: "Chờ duyệt", completed: "Hoàn tất" };
const statusStyle = { draft: "bg-amber-50 text-amber-700", review: "bg-blue-50 text-blue-700", completed: "bg-emerald-50 text-emerald-700" };
const periodStatusLabel = (period: TaxDatabase["periods"][number], transactions: Transaction[]) => period.lockedAt ? "Đã khóa" : period.status === "draft" && !transactions.some(item => item.periodId === period.id) ? "Chưa nhập" : statusLabel[period.status];

function currentDateInputValue() {
  const parts = new Intl.DateTimeFormat("en-US", { timeZone: "Asia/Ho_Chi_Minh", year: "numeric", month: "2-digit", day: "2-digit" }).formatToParts(new Date());
  const value = Object.fromEntries(parts.map(part => [part.type, part.value]));
  return `${value.year}-${value.month}-${value.day}`;
}

function currentMonthLabel() {
  const [year, month] = currentDateInputValue().split("-");
  return `${month}/${year}`;
}

function isValidDateInput(value: string) {
  if (!/^\d{4}-\d{2}-\d{2}$/.test(value)) return false;
  const parsed = new Date(`${value}T00:00:00Z`);
  return !Number.isNaN(parsed.getTime()) && parsed.toISOString().slice(0, 10) === value;
}

function Header({ eyebrow, title, description }: { eyebrow: string; title: string; description?: string }) {
  return <header className="mb-8"><p className="text-xs font-bold uppercase tracking-[.2em] text-pine">{eyebrow}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-ink md:text-4xl">{title}</h1>{description && <p className="mt-2 max-w-2xl text-sm text-slate-500">{description}</p>}</header>;
}

function Dashboard({ data, go }: { data: TaxDatabase; go: (view: View) => void }) {
  if (data.profile.taxpayerType === "enterprise") {
    const valid = data.declarations.filter(item => item.status !== "draft").length;
    const locked = data.declarations.filter(item => item.status === "locked").length;
    return <><Header eyebrow="Tổng quan doanh nghiệp" title={`Xin chào, ${data.profile.legalRepresentative || data.profile.ownerName}`} description={`${data.profile.businessName} · MST ${data.profile.taxCode}`}/><div className="grid gap-4 md:grid-cols-3"><Metric label="Bộ tờ khai" value={`${data.declarations.length} hồ sơ`} note="GTGT, TNCN và TNDN" accent="bg-pine"/><Metric label="Đã kiểm tra" value={`${valid} hồ sơ`} note="Đã lưu và qua kiểm tra cơ bản" accent="bg-coral"/><Metric label="Đã khóa" value={`${locked} hồ sơ`} note="Không thể chỉnh sửa" accent="bg-amber-400"/></div><button onClick={() => go("declarations")} className="card group mt-6 w-full text-left"><p className="text-xs font-bold uppercase tracking-wider text-pine">Hồ sơ doanh nghiệp</p><h2 className="mt-3 text-2xl font-semibold text-ink group-hover:text-pine">Lập tờ khai GTGT, TNCN, TNDN →</h2><p className="mt-2 text-sm text-slate-500">Nhập chỉ tiêu, kiểm tra dữ liệu và khóa tờ khai theo kỳ.</p></button></>;
  }
  const calculations = data.periods.map((period) => period.taxSnapshot ?? calculateTax(data.profile, period, data.transactions));
  const revenue = calculations.reduce((sum, item) => sum + item.revenue, 0);
  const expenses = calculations.reduce((sum, item) => sum + item.expenses, 0);
  const tax = calculations.reduce((sum, item) => sum + item.totalTax, 0);
  const nextDue = [...data.periods].filter(period => period.status !== "completed").sort((a, b) => a.dueDate.localeCompare(b.dueDate))[0];
  return <><Header eyebrow="Tổng quan năm 2026" title={`Chào buổi sáng, ${data.profile.ownerName.split(" ").at(-1)}!`} description="Bức tranh nhanh về hoạt động kinh doanh và nghĩa vụ thuế hiện tại."/>
    {nextDue && <button onClick={() => go("periods")} className="mb-5 flex w-full items-center justify-between rounded-2xl border border-amber-200 bg-amber-50 p-4 text-left"><span className="text-sm text-amber-900"><strong>Nhắc hạn:</strong> {nextDue.label} cần hoàn tất trước {new Date(nextDue.dueDate).toLocaleDateString("vi-VN")}</span><span className="text-amber-700">→</span></button>}
    <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">
      <Metric label="Tổng doanh thu" value={formatVnd(revenue)} note="Từ các kỳ đã ghi nhận" accent="bg-pine"/>
      <Metric label="Lợi nhuận tạm tính" value={formatVnd(revenue - expenses)} note={`${formatVnd(expenses)} chi phí`} accent="bg-blue-500"/>
      <Metric label="Thuế dự kiến" value={formatVnd(tax)} note="GTGT + Thuế thu nhập" accent="bg-coral"/>
      <Metric label="Kỳ chưa hoàn tất" value={`${data.periods.filter((p) => p.status !== "completed").length} kỳ`} note="Cần tiếp tục xử lý" accent="bg-amber-400"/>
    </div>
    <section className="card mt-6"><div className="flex items-center justify-between"><div><h2 className="section-title">Kỳ kê khai gần đây</h2><p className="section-subtitle">Theo dõi tiến độ và hạn nộp</p></div><button className="link-button" onClick={() => go("periods")}>Xem tất cả →</button></div>
      <div className="mt-5 overflow-x-auto"><table><thead><tr><th>Kỳ kê khai</th><th>Doanh thu</th><th>Thuế dự kiến</th><th>Hạn nộp</th><th>Trạng thái</th></tr></thead><tbody>{data.periods.map((period, i) => <tr key={period.id}><td className="font-semibold text-ink">{period.label}</td><td>{formatVnd(calculations[i].revenue)}</td><td>{formatVnd(calculations[i].totalTax)}</td><td>{new Date(period.dueDate).toLocaleDateString("vi-VN")}</td><td><span className={`badge ${statusStyle[period.status]}`}>{periodStatusLabel(period, data.transactions)}</span></td></tr>)}</tbody></table></div>
    </section>
    <div className="mt-6 grid gap-4 md:grid-cols-2"><button onClick={() => go("transactions")} className="card group text-left"><p className="text-xs font-bold uppercase tracking-wider text-coral">Tiếp tục kỳ hiện tại</p><h3 className="mt-3 text-xl font-semibold text-ink group-hover:text-pine">Nhập doanh thu Quý 3/2026 →</h3><p className="mt-2 text-sm text-slate-500">Bổ sung hóa đơn và rà soát giao dịch trước khi tính thuế.</p></button><button onClick={() => go("import")} className="card group text-left"><p className="text-xs font-bold uppercase tracking-wider text-pine">Thao tác nhanh</p><h3 className="mt-3 text-xl font-semibold text-ink group-hover:text-pine">Import dữ liệu từ Excel →</h3><p className="mt-2 text-sm text-slate-500">Tải file mẫu, kiểm tra lỗi và xác nhận nhập dữ liệu.</p></button></div>
  </>;
}

function Metric({ label, value, note, accent }: { label: string; value: string; note: string; accent: string }) {
  return <div className="card relative overflow-hidden"><div className={`absolute left-0 top-0 h-full w-1 ${accent}`}/><p className="text-sm text-slate-500">{label}</p><p className="mt-3 text-2xl font-semibold text-ink">{value}</p><p className="mt-2 text-xs text-slate-400">{note}</p></div>;
}

function Profile({ data, refresh }: { data: TaxDatabase; refresh: () => Promise<void> }) {
  const [form, setForm] = useState<BusinessProfile>(data.profile); const [saved, setSaved] = useState(false);
  async function submit(e: FormEvent) { e.preventDefault(); await taxApi.updateProfile(form); await refresh(); setSaved(true); }
  const enterprise = form.taxpayerType === "enterprise";
  return <><Header eyebrow="Thiết lập" title={enterprise ? "Hồ sơ doanh nghiệp" : "Hồ sơ hộ kinh doanh"} description="Loại người nộp thuế quyết định bộ tờ khai và engine nghiệp vụ được sử dụng."/><form onSubmit={submit} className="card grid gap-5 md:grid-cols-2">
    <Field label="Đối tượng nộp thuế" wide><select className="field" value={form.taxpayerType} onChange={(e) => setForm({...form, taxpayerType: e.target.value as BusinessProfile["taxpayerType"]})}><option value="household">Hộ kinh doanh</option><option value="enterprise">Doanh nghiệp / công ty</option></select></Field>
    <Field label={enterprise ? "Tên doanh nghiệp" : "Tên hộ kinh doanh"}><input className="field" value={form.businessName} onChange={(e) => setForm({...form, businessName: e.target.value})}/></Field><Field label="Mã số thuế"><input className="field" value={form.taxCode} onChange={(e) => setForm({...form, taxCode: e.target.value})}/></Field>
    <Field label={enterprise ? "Người đại diện pháp luật" : "Chủ hộ kinh doanh"}><input className="field" value={enterprise ? form.legalRepresentative ?? "" : form.ownerName} onChange={(e) => setForm(enterprise ? {...form, legalRepresentative: e.target.value} : {...form, ownerName: e.target.value})}/></Field><Field label="Kỳ kê khai"><select className="field" value={form.declarationKind} onChange={(e) => setForm({...form, declarationKind: e.target.value as "month"|"quarter"})}><option value="quarter">Theo quý</option><option value="month">Theo tháng</option></select></Field>
    {enterprise ? <><Field label="Loại hình doanh nghiệp"><select className="field" value={form.enterpriseType ?? "limited"} onChange={(e) => setForm({...form, enterpriseType: e.target.value as BusinessProfile["enterpriseType"]})}><option value="limited">Công ty TNHH</option><option value="joint_stock">Công ty cổ phần</option><option value="private">Doanh nghiệp tư nhân</option><option value="partnership">Công ty hợp danh</option></select></Field><Field label="Phương pháp GTGT"><select className="field" value={form.vatMethod ?? "deduction"} onChange={(e) => setForm({...form, vatMethod: e.target.value as BusinessProfile["vatMethod"]})}><option value="deduction">Khấu trừ</option><option value="direct">Trực tiếp</option></select></Field><Field label="Chế độ kế toán" wide><select className="field" value={form.accountingRegime ?? "circular_133"} onChange={(e) => setForm({...form, accountingRegime: e.target.value as BusinessProfile["accountingRegime"]})}><option value="circular_133">Thông tư 133</option><option value="circular_200">Thông tư 200</option></select></Field></> : <Field label="Phương pháp tính thuế / nhóm sổ" wide><select className="field" value={form.householdTaxMethod ?? "revenue_percentage"} onChange={(e) => setForm({...form, householdTaxMethod: e.target.value as BusinessProfile["householdTaxMethod"]})}><option value="non_taxable">Không chịu GTGT, không nộp TNCN · S1a-HKD</option><option value="revenue_percentage">Thuế theo tỷ lệ trên doanh thu · S2a-HKD</option><option value="taxable_income">TNCN trên thu nhập tính thuế · S2b–S2e-HKD</option></select></Field>}
    <Field label="Địa chỉ" wide><input className="field" value={form.address} onChange={(e) => setForm({...form, address: e.target.value})}/></Field><Field label="Ngành nghề" wide><select className="field" value={form.industry} onChange={(e) => setForm({...form, industry: e.target.value as BusinessProfile["industry"]})}><option value="distribution">Phân phối, cung cấp hàng hóa</option><option value="services">Dịch vụ</option><option value="production">Sản xuất, vận tải</option><option value="other">Kinh doanh khác</option></select></Field>
    <div className="flex items-center gap-3 md:col-span-2"><button className="primary">Lưu hồ sơ</button>{saved && <span className="text-sm text-pine">Đã lưu thay đổi</span>}</div>
  </form></>;
}

function Field({ label, wide, children }: { label: string; wide?: boolean; children: React.ReactNode }) { return <label className={`text-sm font-medium text-ink ${wide ? "md:col-span-2" : ""}`}><span className="mb-2 block">{label}</span>{children}</label>; }

function Periods({ data, go }: { data: TaxDatabase; go: (view: View) => void }) {
  return <><Header eyebrow="Kê khai" title="Danh sách kỳ kê khai" description="Quản lý tiến độ theo tháng hoặc quý. Kỳ chưa có giao dịch hiển thị Chưa nhập; chỉ Chờ duyệt sau khi bạn chủ động gửi duyệt."/><section className="card overflow-x-auto"><table><thead><tr><th>Kỳ</th><th>Loại</th><th>Hạn nộp</th><th>Đã nộp</th><th>Trạng thái</th><th></th></tr></thead><tbody>{data.periods.map((period) => <tr key={period.id}><td className="font-semibold text-ink">{period.label}{period.lockedAt && <span className="ml-2 text-xs text-slate-400">🔒</span>}</td><td>{period.kind === "quarter" ? "Theo quý" : "Theo tháng"}</td><td>{new Date(period.dueDate).toLocaleDateString("vi-VN")}</td><td>{formatVnd(period.paidAmount)}</td><td><span className={`badge ${statusStyle[period.status]}`}>{periodStatusLabel(period, data.transactions)}</span></td><td><button className="link-button" onClick={() => go("result")}>Xem kết quả</button></td></tr>)}</tbody></table></section></>;
}

function PeriodPicker({ data, activePeriodId, selectPeriod, go }: { data: TaxDatabase; activePeriodId: string; selectPeriod: (periodId: string) => void; go: (view: View) => void }) {
  const periods = [...data.periods].sort((a, b) => a.id.localeCompare(b.id));
  return <section className="card mb-5"><h2 className="section-title">Chọn kỳ muốn nhập dữ liệu</h2><p className="section-subtitle">Kỳ đã chọn được ghi nhớ cho giao dịch, import, tính thuế và xuất Excel.</p><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{periods.map(period => <button key={period.id} disabled={Boolean(period.lockedAt)} onClick={() => { selectPeriod(period.id); go("transactions"); }} className={`rounded-xl border p-4 text-left transition disabled:opacity-50 ${period.id === activePeriodId ? "border-pine bg-mint/60 ring-2 ring-mint" : "hover:border-pine"}`}><div className="flex items-center justify-between"><span className="font-semibold text-ink">{period.label}</span>{period.id === activePeriodId && <span className="badge bg-pine text-white">Đang chọn</span>}</div><p className="mt-2 text-xs text-slate-500">{periodStatusLabel(period, data.transactions)} · Hạn {new Date(period.dueDate).toLocaleDateString("vi-VN")}</p><p className="mt-3 text-sm font-semibold text-pine">{period.lockedAt ? "Kỳ đã khóa" : "Nhập dữ liệu kỳ này →"}</p></button>)}</div></section>;
}

function Transactions({ data, refresh }: { data: TaxDatabase; refresh: () => Promise<void> }) {
  const [amount, setAmount] = useState(""); const [date, setDate] = useState(currentDateInputValue); const [accountingDate, setAccountingDate] = useState(currentDateInputValue); const [description, setDescription] = useState(""); const [category, setCategory] = useState<BusinessProfile["industry"]>(data.profile.industry); const [receiptNo, setReceiptNo] = useState(""); const [contactName, setContactName] = useState(""); const [debitAccount, setDebitAccount] = useState("1111"); const [creditAccount, setCreditAccount] = useState("511"); const [currency, setCurrency] = useState("VND"); const [exchangeRate, setExchangeRate] = useState("1"); const [invoiceNo, setInvoiceNo] = useState(""); const [invoiceDate, setInvoiceDate] = useState(""); const [caseCode, setCaseCode] = useState(""); const [collector, setCollector] = useState(""); const [note, setNote] = useState(""); const [attachments, setAttachments] = useState<import("@/domain/tax").ReceiptAttachment[]>([]); const [editingReceiptId, setEditingReceiptId] = useState("");
  const [counterpartyCode, setCounterpartyCode] = useState(""); const [counterpartyName, setCounterpartyName] = useState(""); const [counterpartyTaxCode, setCounterpartyTaxCode] = useState(""); const [counterpartyAddress, setCounterpartyAddress] = useState(""); const [saveCounterparty, setSaveCounterparty] = useState(false);
  const [counterparties, setCounterparties] = useState<import("@/domain/tax").Counterparty[]>([]); const [lookupState, setLookupState] = useState<"idle" | "loading" | "not_found" | "error">("idle"); const [lookupMessage, setLookupMessage] = useState(""); const [saving, setSaving] = useState(false);
  const [editingDateId, setEditingDateId] = useState(""); const [editingDate, setEditingDate] = useState(""); const [message, setMessage] = useState("");
  useEffect(() => { let active = true; void taxApi.getCounterparties().then((items) => { if (active) setCounterparties(items); }).catch(() => { if (active) { setLookupState("error"); setLookupMessage("Không tải được danh mục đối tượng; vẫn có thể nhập mã thủ công."); } }); return () => { active = false; }; }, []);
  async function lookupCounterparty() {
    const code = counterpartyCode.trim(); if (!code) { setLookupState("idle"); setLookupMessage(""); return; }
    setLookupState("loading"); setLookupMessage("");
    try { const item = await taxApi.getCounterparty(code); setCounterpartyName(item.name); setCounterpartyTaxCode(item.taxCode); setCounterpartyAddress(item.address); setLookupState("idle"); }
    catch (cause) { const detail = cause instanceof Error ? cause.message : "Không thể tra cứu đối tượng"; if (detail.toLowerCase().includes("không tìm thấy")) { setLookupState("not_found"); setLookupMessage("Không tìm thấy đối tượng. Nhập thông tin bên dưới và chọn lưu danh mục nếu cần."); } else { setLookupState("error"); setLookupMessage(detail); } }
  }
  async function submit(e: FormEvent, status: "draft" | "saved" = "saved") {
    e.preventDefault(); setMessage(""); const convertedAmount = Math.round(parseVnd(amount) * Number(exchangeRate));
    if (!parseVnd(amount) || !description.trim() || !receiptNo.trim() || !counterpartyCode.trim() || !counterpartyName.trim() || !isValidDateInput(date) || !isValidDateInput(accountingDate) || !/^\d{3,10}$/.test(debitAccount) || !/^\d{3,10}$/.test(creditAccount) || !Number.isFinite(Number(exchangeRate)) || Number(exchangeRate) <= 0) { setMessage("Kiểm tra trường bắt buộc: ngày, tài khoản Nợ/Có hợp lệ và số tiền lớn hơn 0."); return; }
    try {
      setSaving(true); const payload = { periodId: data.periods[0].id, voucherDate: date, accountingDate, status, receiptNo, counterpartyCode, counterpartyName, counterpartyTaxCode, counterpartyAddress, description, contactName, debitAccount, creditAccount, amount: parseVnd(amount), currency, exchangeRate: Number(exchangeRate), convertedAmount, revenueCategory: category, invoiceNo, invoiceDate, caseCode, collector, note, attachments, saveCounterparty };
      if (editingReceiptId) await taxApi.updateCashReceipt({ ...payload, id: editingReceiptId }); else await taxApi.createCashReceipt(payload);
      setAmount(""); setDescription(""); setReceiptNo(""); setCounterpartyCode(""); setCounterpartyName(""); setCounterpartyTaxCode(""); setCounterpartyAddress(""); setContactName(""); setCreditAccount(""); setInvoiceNo(""); setInvoiceDate(""); setCaseCode(""); setCollector(""); setNote(""); setAttachments([]); setEditingReceiptId(""); setSaveCounterparty(false); setLookupState("idle"); await refresh();
    } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Không thể lưu phiếu thu"); } finally { setSaving(false); }
  }
  async function addAttachments(files?: FileList | null) { if (!files) return; const selected = Array.from(files); if (selected.length + attachments.length > 5 || selected.reduce((sum, file) => sum + file.size, attachments.reduce((sum, file) => sum + file.size, 0)) > 5 * 1024 * 1024) { setMessage("Chỉ đính kèm tối đa 5 tệp, tổng dung lượng 5 MB."); return; } setAttachments([...attachments, ...await Promise.all(selected.map(async file => ({ name: file.name, type: file.type, size: file.size, data: await new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = reject; reader.readAsDataURL(file); }) })))]) }
  function editReceipt(item: Transaction) { const detail = item.cashReceipt; if (!detail) return; setEditingReceiptId(item.id); setDate(detail.voucherDate); setAccountingDate(detail.accountingDate); setReceiptNo(item.documentNo || item.invoiceNo); setCounterpartyCode(item.counterpartyCode ?? ""); setCounterpartyName(item.counterpartyName ?? ""); setCounterpartyTaxCode(item.counterpartyTaxCode ?? ""); setCounterpartyAddress(item.counterpartyAddress ?? ""); setDescription(item.description); setContactName(detail.contactName); setDebitAccount(detail.debitAccount); setCreditAccount(detail.creditAccount); setAmount(String(item.amount)); setCurrency(detail.currency); setExchangeRate(String(detail.exchangeRate)); setInvoiceNo(detail.invoiceNo); setInvoiceDate(detail.invoiceDate); setCaseCode(detail.caseCode); setCollector(detail.collector); setNote(detail.note); setAttachments(detail.attachments); window.scrollTo({ top: 0, behavior: "smooth" }); }
  async function saveDate(item: Transaction) {
    setMessage("");
    if (!isValidDateInput(editingDate)) { setMessage("Ngày giao dịch không hợp lệ."); return; }
    try { await taxApi.updateTransaction({ ...item, date: editingDate, vatAmount: item.vatAmount ?? 0 }); await refresh(); setEditingDateId(""); }
    catch (cause) { setMessage(cause instanceof Error ? cause.message : "Không thể cập nhật ngày giao dịch"); }
  }
  const items = data.transactions.filter((item) => item.periodId === data.periods[0].id);
  const issues = validateTransactions(items);
  return <><Header eyebrow="Chứng từ" title="Nhập chứng từ phát sinh" description={`Đang nhập dữ liệu cho ${data.periods[0].label}. Phiếu thu tiền mặt được ghi nhận vào doanh thu và sổ tiền hiện có.`}/>{issues.length > 0 && <div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">Có {issues.length} cảnh báo thiếu, sai hoặc trùng dữ liệu cần rà soát.</div>}{message && <p className="mb-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{message}</p>}<section className="card mb-6"><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Field label="Loại chứng từ" wide><select className="field" value="cash_receipt"><option value="cash_receipt">Phiếu thu tiền mặt</option><option disabled>Phiếu chi tiền mặt · Phát triển sau</option><option disabled>Giấy báo Có · Phát triển sau</option><option disabled>Giấy báo Nợ · Phát triển sau</option></select></Field></div><form onSubmit={submit} className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Field label="Ngày phiếu thu"><input required type="date" className="field" value={date} onChange={(e) => setDate(e.target.value)}/></Field><Field label="Số phiếu thu"><input required className="field" placeholder="PT-0001" value={receiptNo} onChange={(e) => setReceiptNo(e.target.value)}/></Field><Field label="Mã đối tượng"><input required list="counterparty-codes" className="field" placeholder="Nhập hoặc chọn mã" value={counterpartyCode} onChange={(e) => { setCounterpartyCode(e.target.value); setLookupState("idle"); setLookupMessage(""); }} onBlur={() => void lookupCounterparty()}/><datalist id="counterparty-codes">{counterparties.map(item => <option key={item.code} value={item.code} label={item.name}/>)}</datalist>{lookupState === "loading" && <span className="mt-1 block text-xs text-slate-500">Đang tải thông tin đối tượng…</span>}{lookupState === "not_found" && <span className="mt-1 block text-xs text-amber-700">{lookupMessage}</span>}{lookupState === "error" && <span className="mt-1 block text-xs text-red-600">{lookupMessage}</span>}</Field><Field label="Tên đối tượng"><input required className="field" value={counterpartyName} onChange={(e) => setCounterpartyName(e.target.value)}/></Field><Field label="Mã số thuế"><input className="field" value={counterpartyTaxCode} onChange={(e) => setCounterpartyTaxCode(e.target.value)}/></Field><Field label="Địa chỉ" wide><input className="field" value={counterpartyAddress} onChange={(e) => setCounterpartyAddress(e.target.value)}/></Field><Field label="Nội dung thu"><input required className="field" placeholder="Nội dung phiếu thu" value={description} onChange={(e) => setDescription(e.target.value)}/></Field><Field label="Nhóm ngành"><select className="field" value={category} onChange={(e) => setCategory(e.target.value as BusinessProfile["industry"])}><option value="distribution">Phân phối</option><option value="services">Dịch vụ</option><option value="production">Sản xuất, vận tải</option><option value="other">Nhóm khác</option></select></Field><Field label="Số tiền thu (VND)"><input required className="field" inputMode="numeric" placeholder="0" value={amount} onChange={(e) => setAmount(e.target.value)}/></Field><label className="flex items-end gap-2 pb-3 text-sm text-ink"><input type="checkbox" checked={saveCounterparty} onChange={(e) => setSaveCounterparty(e.target.checked)}/> Lưu/cập nhật địa chỉ vào danh mục</label><div className="flex items-end"><button disabled={Boolean(data.periods[0].lockedAt) || saving} className="primary w-full disabled:opacity-40">{saving ? "Đang lưu…" : "Lập phiếu thu"}</button></div></form></section><section className="card overflow-x-auto"><table><thead><tr><th>Ngày</th><th>Loại</th><th>Nhóm ngành</th><th>Nội dung</th><th>Hóa đơn</th><th className="text-right">Số tiền</th><th className="text-right">Thuế GTGT</th><th></th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className={issues.some(issue => issue.transactionId === item.id) ? "bg-amber-50/50" : ""}><td>{editingDateId === item.id ? <input aria-label={`Ngày của ${item.description}`} type="date" className="field min-w-36" value={editingDate} onChange={(e) => setEditingDate(e.target.value)}/> : new Date(`${item.date}T00:00:00`).toLocaleDateString("vi-VN")}</td><td><span className={`badge ${item.type === "revenue" ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-600"}`}>{item.voucherType === "cash_receipt" ? "Phiếu thu" : item.type === "revenue" ? "Doanh thu" : "Chi phí"}</span></td><td>{item.revenueCategory}</td><td className="font-medium text-ink">{item.description}</td><td>{item.documentNo || item.invoiceNo}</td><td className="text-right font-semibold">{formatVnd(item.amount)}</td><td className="text-right">{formatVnd(item.vatAmount ?? 0)}</td><td>{editingDateId === item.id ? <div className="flex gap-2"><button disabled={Boolean(data.periods[0].lockedAt)} onClick={() => void saveDate(item)} className="link-button">Lưu</button><button onClick={() => setEditingDateId("")} className="text-xs text-slate-500">Hủy</button></div> : <button disabled={Boolean(data.periods[0].lockedAt)} onClick={() => { setEditingDateId(item.id); setEditingDate(item.date); setMessage(""); }} className="link-button disabled:opacity-40">Sửa ngày</button>}</td></tr>)}</tbody></table></section></>;
}

function TransactionRemoval({ data, refresh }: { data: TaxDatabase; refresh: () => Promise<void> }) {
  const items = data.transactions.filter(item => item.periodId === data.periods[0].id);
  const [error, setError] = useState("");
  if (!items.length) return null;
  async function remove(item: Transaction) {
    if (!window.confirm(`Xóa giao dịch “${item.description}” (${formatVnd(item.amount)})?`)) return;
    setError("");
    try { await taxApi.deleteTransaction(item.id); await refresh(); }
    catch (cause) { setError(cause instanceof Error ? cause.message : "Không thể xóa giao dịch"); }
  }
  if (error) return <section className="card mt-5"><p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p><button onClick={() => setError("")} className="secondary mt-3">Đóng thông báo</button></section>;
  return <section className="card mt-5"><h2 className="section-title">Xóa giao dịch nhập sai</h2><p className="section-subtitle">Chỉ thực hiện được trước khi khóa kỳ; thao tác xóa được lưu trong lịch sử.</p><div className="mt-4 divide-y">{items.map(item => <div key={item.id} className="flex items-center justify-between gap-4 py-3"><div><p className="text-sm font-medium text-ink">{item.description}</p><p className="mt-1 text-xs text-slate-400">{item.invoiceNo} · {formatVnd(item.amount)}</p></div><button disabled={Boolean(data.periods[0].lockedAt)} onClick={() => void remove(item)} className="rounded-lg px-3 py-2 text-sm font-semibold text-red-600 hover:bg-red-50 disabled:opacity-40">Xóa</button></div>)}</div></section>;
}

type PreviewRow = Transaction & { error?: string };

function ImportExcel({ data, refresh }: { data: TaxDatabase; refresh: () => Promise<void> }) {
  const inputRef = useRef<HTMLInputElement>(null); const [rows, setRows] = useState<PreviewRow[]>([]); const [message, setMessage] = useState("");
  async function downloadTemplate() {
    const ExcelJS = await import("exceljs"); const book = new ExcelJS.Workbook(); const sheet = book.addWorksheet("Giao dich");
    sheet.columns = [{ header: "ngay", key: "ngay", width: 14 }, { header: "loai", key: "loai", width: 16 }, { header: "nhom_nganh", key: "nhom_nganh", width: 18 }, { header: "noi_dung", key: "noi_dung", width: 32 }, { header: "so_hoa_don", key: "so_hoa_don", width: 18 }, { header: "so_chung_tu", key: "so_chung_tu", width: 18 }, { header: "so_tien_vnd", key: "so_tien_vnd", width: 18 }, { header: "thue_gtgt_vnd", key: "thue_gtgt_vnd", width: 18 }];
    sheet.addRow({ ngay: "2026-08-01", loai: "doanh_thu", nhom_nganh: "distribution", noi_dung: "Ví dụ bán hàng", so_hoa_don: "HD-001", so_chung_tu: "", so_tien_vnd: 1000000, thue_gtgt_vnd: 80000 });
    downloadBuffer(await book.xlsx.writeBuffer(), "mau-import-thue.xlsx");
  }
  async function selectFile(file?: File) {
    if (!file) return; const ExcelJS = await import("exceljs"); const book = new ExcelJS.Workbook(); await book.xlsx.load(await file.arrayBuffer()); const sheet = book.worksheets[0]; const headers = (sheet.getRow(1).values as unknown[]).slice(1).map(String); const raw: Record<string, unknown>[] = [];
    sheet.eachRow((row, rowNumber) => { if (rowNumber === 1) return; const values = (row.values as unknown[]).slice(1); raw.push(Object.fromEntries(headers.map((header, index) => [header, values[index]]))); });
    const seenKeys = new Set(data.transactions.map(item => `${item.date}|${item.invoiceNo.trim().toLowerCase()}|${item.amount}`));
    setRows(raw.map((row, index) => { const amount = Number(row.so_tien_vnd); const vatAmount = row.thue_gtgt_vnd === undefined || row.thue_gtgt_vnd === "" ? 0 : Number(row.thue_gtgt_vnd); const date = String(row.ngay ?? ""); const category = String(row.nhom_nganh ?? ""); const invoiceNo = String(row.so_hoa_don ?? ""); const key = `${date}|${invoiceNo.trim().toLowerCase()}|${amount}`; const duplicate = seenKeys.has(key); seenKeys.add(key); const error = !isValidDateInput(date) ? "Ngày phải có dạng YYYY-MM-DD hợp lệ" : !Number.isSafeInteger(amount) || amount <= 0 ? "Số tiền phải là số nguyên dương" : !Number.isSafeInteger(vatAmount) || vatAmount < 0 ? "Thuế GTGT phải là số nguyên không âm" : !["doanh_thu", "chi_phi"].includes(String(row.loai)) ? "Loại không hợp lệ" : !["distribution", "services", "production", "other"].includes(category) ? "Nhóm ngành không hợp lệ" : !invoiceNo ? "Thiếu số hóa đơn/chứng từ" : duplicate ? "Trùng giao dịch đã có hoặc trong file" : undefined; return { id: crypto.randomUUID(), periodId: data.periods[0].id, date, type: row.loai === "chi_phi" ? "expense" : "revenue", revenueCategory: category as BusinessProfile["industry"], description: String(row.noi_dung ?? "Dòng " + (index + 2)), invoiceNo, documentNo: String(row.so_chung_tu ?? ""), amount: Number.isFinite(amount) ? amount : 0, vatAmount: Number.isFinite(vatAmount) ? vatAmount : 0, paymentStatus: "paid", outstandingAmount: 0, error }; })); setMessage("");
  }
  async function confirm() { const valid = rows.filter((row) => !row.error).map(({ error: _, ...row }) => row); if (!valid.length || valid.length !== rows.length || data.periods[0].lockedAt) return; await taxApi.confirmImport(valid); await refresh(); setMessage(`Đã nhập thành công ${valid.length} giao dịch.`); setRows([]); }
  return <><Header eyebrow="Nhập dữ liệu" title="Import giao dịch từ Excel" description="Dùng đúng file mẫu để kiểm tra dữ liệu trước khi ghi vào kỳ kê khai."/>{data.periods[0].lockedAt && <p className="mb-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-800">Kỳ hiện tại đã khóa; chỉ có thể xem và xuất dữ liệu.</p>}<div className="grid gap-5 lg:grid-cols-[1fr_1.7fr]"><section className="card"><span className="grid h-12 w-12 place-items-center rounded-xl bg-mint text-xl font-bold text-pine">1</span><h2 className="mt-5 section-title">Chuẩn bị file dữ liệu</h2><p className="mt-2 text-sm leading-6 text-slate-500">Tải file mẫu gồm ngày, loại giao dịch, nhóm ngành, hóa đơn, chứng từ, số tiền và thuế GTGT nguyên VND.</p><button onClick={downloadTemplate} className="secondary mt-5">↓ Tải file mẫu Excel</button><div className="my-7 border-t"/><span className="grid h-12 w-12 place-items-center rounded-xl bg-orange-50 text-xl font-bold text-coral">2</span><h2 className="mt-5 section-title">Chọn file đã điền</h2><input ref={inputRef} className="hidden" type="file" accept=".xlsx,.xls" onChange={(e) => void selectFile(e.target.files?.[0])}/><button disabled={Boolean(data.periods[0].lockedAt)} onClick={() => inputRef.current?.click()} className="primary mt-5 w-full disabled:opacity-40">Chọn file Excel</button>{message && <p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{message}</p>}</section><section className="card min-w-0"><div className="flex items-center justify-between"><div><h2 className="section-title">Xem trước & kiểm tra</h2><p className="section-subtitle">{rows.length ? `${rows.length} dòng · ${rows.filter(r => r.error).length} lỗi` : "Chưa có dữ liệu"}</p></div>{rows.length > 0 && <button disabled={rows.some(r => r.error) || Boolean(data.periods[0].lockedAt)} onClick={() => void confirm()} className="primary disabled:cursor-not-allowed disabled:opacity-40">Xác nhận import</button>}</div>{rows.length === 0 ? <div className="mt-8 grid min-h-56 place-items-center rounded-2xl border border-dashed border-slate-200 bg-slate-50 text-center text-sm text-slate-400">Dữ liệu xem trước sẽ xuất hiện ở đây</div> : <div className="mt-5 overflow-x-auto"><table><thead><tr><th>Dòng</th><th>Ngày</th><th>Nhóm ngành</th><th>Nội dung</th><th>Số tiền</th><th>Thuế GTGT</th><th>Kiểm tra</th></tr></thead><tbody>{rows.map((row, i) => <tr key={row.id}><td>{i+2}</td><td>{row.date}</td><td>{row.revenueCategory}</td><td>{row.description}</td><td>{formatVnd(row.amount)}</td><td>{formatVnd(row.vatAmount)}</td><td>{row.error ? <span className="text-xs text-red-600">{row.error}</span> : <span className="text-emerald-600">Hợp lệ</span>}</td></tr>)}</tbody></table></div>}</section></div></>;
}

function ElectronicInvoiceImport() {
  const input = useRef<HTMLInputElement>(null);
  const [result, setResult] = useState<{ valid: number; invalid: number } | null>(null);
  async function inspect(files?: FileList | null) {
    if (!files?.length) return;
    const checks = await Promise.all(Array.from(files).map(async file => {
      try { const content = await file.text(); if (file.name.toLowerCase().endsWith(".json")) { JSON.parse(content); return true; } return file.name.toLowerCase().endsWith(".xml") && content.trimStart().startsWith("<"); }
      catch { return false; }
    }));
    setResult({ valid: checks.filter(Boolean).length, invalid: checks.filter(value => !value).length });
  }
  return <section className="card mt-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wider text-pine">Hóa đơn điện tử</p><h2 className="mt-1 section-title">Nhập file XML/JSON</h2><p className="mt-2 text-sm text-slate-500">Kiểm tra định dạng trước khi ánh xạ vào doanh thu, chi phí và công nợ.</p></div><input ref={input} type="file" accept=".xml,.json" multiple className="hidden" onChange={(event) => void inspect(event.target.files)}/><button onClick={() => input.current?.click()} className="secondary">Chọn hóa đơn điện tử</button></div>{result && <div className="mt-4 rounded-xl bg-slate-50 p-4 text-sm"><span className="text-emerald-700">{result.valid} file hợp lệ định dạng</span>{result.invalid > 0 && <span className="ml-4 text-red-600">{result.invalid} file lỗi</span>}<p className="mt-2 text-xs text-slate-400">Cần bổ sung mapping theo nhà cung cấp/schema hóa đơn trước khi xác nhận ghi sổ.</p></div>}</section>;
}

function Result({ data, refresh }: { data: TaxDatabase; refresh: () => Promise<void> }) {
  const [periodId, setPeriodId] = useState(data.periods[0].id);
  const period = data.periods.find(p => p.id === periodId)!;
  const localResult = useMemo(() => period.taxSnapshot ?? calculateTax(data.profile, period, data.transactions), [data, period]);
  const [apiResult, setApiResult] = useState<TaxBreakdown | null>(period.taxSnapshot ?? null);
  useEffect(() => {
    let active = true;
    void taxApi.calculate(period.id).then((value) => { if (active) setApiResult(value); }).catch(() => { if (active) setApiResult(null); });
    return () => { active = false; };
  }, [period.id, period.taxSnapshot]);
  const result = apiResult?.periodId === period.id ? apiResult : localResult;
  const issues = validateTransactions(data.transactions.filter(item => item.periodId === period.id));
  async function lock() { if (issues.length) return; await taxApi.lockPeriod(period.id); await refresh(); }
  return <><Header eyebrow="Tính thuế" title="Kết quả & công thức" description="Tính theo từng nhóm ngành; kỳ đã khóa luôn đọc snapshot công thức cũ."/><div className="mb-5 flex justify-end"><select className="field max-w-xs" value={periodId} onChange={(e) => setPeriodId(e.target.value)}>{data.periods.map(p => <option key={p.id} value={p.id}>{p.label}</option>)}</select></div><div className="grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><section className="card"><p className="text-xs font-bold uppercase tracking-wider text-pine">Cơ sở tính thuế</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><ResultBox label="Tổng doanh thu" value={formatVnd(result.revenue)}/><ResultBox label="Chi phí ghi nhận" value={formatVnd(result.expenses)}/></div><div className="my-7 border-t"/><h2 className="section-title">Chi tiết theo nhóm ngành</h2>{result.lines.map(line => <div key={line.ruleId}><Formula label={`${line.industry} · GTGT`} formula={`${formatVnd(line.revenue)} × ${line.vatRateBps / 100}%`} result={line.vat}/><Formula label={`${line.industry} · Thuế thu nhập`} formula={`${formatVnd(line.revenue)} × ${line.pitRateBps / 100}%`} result={line.pit}/></div>)}<p className="mt-5 rounded-xl bg-amber-50 p-4 text-xs leading-5 text-amber-800">Cấu hình {result.formulaVersion}. {period.lockedAt ? `Snapshot khóa lúc ${new Date(period.lockedAt).toLocaleString("vi-VN")}.` : "Tỷ lệ minh họa, cần nghiệp vụ xác nhận trước khi dùng thật."}</p></section><section className="rounded-3xl bg-ink p-7 text-white shadow-card"><p className="text-xs uppercase tracking-[.18em] text-white/50">Nghĩa vụ kỳ này</p><p className="mt-3 text-3xl font-semibold">{formatVnd(result.totalTax)}</p><div className="my-7 border-t border-white/10"/><Summary label="Thuế GTGT" value={result.vat}/><Summary label="Thuế thu nhập" value={result.pit}/><Summary label="Đã nộp" value={result.paid}/><div className="my-5 border-t border-white/10"/><div className="flex items-end justify-between"><span className="text-sm text-white/60">Còn phải nộp</span><span className="text-2xl font-semibold text-coral">{formatVnd(result.remaining)}</span></div>{issues.length > 0 && <p className="mt-5 text-xs text-amber-300">Còn {issues.length} cảnh báo; chưa thể khóa kỳ.</p>}<button disabled={Boolean(period.lockedAt) || issues.length > 0} onClick={() => void lock()} className="mt-7 w-full rounded-xl bg-white px-4 py-3 font-semibold text-ink disabled:opacity-40">{period.lockedAt ? "Kỳ đã khóa" : "Khóa kỳ & lưu snapshot"}</button><p className="mt-4 text-center text-xs text-white/40">Không tự động nộp lên Thuế điện tử</p></section></div></>;
}
function ResultBox({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl bg-slate-50 p-5"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-xl font-semibold text-ink">{value}</p></div>; }
function Formula({ label, formula, result }: { label: string; formula: string; result: number }) { return <div className="mt-4 flex flex-col gap-2 rounded-2xl border p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold text-ink">{label}</p><p className="mt-1 text-xs text-slate-400">{formula}</p></div><p className="font-semibold text-pine">{formatVnd(result)}</p></div>; }
function Summary({ label, value }: { label: string; value: number }) { return <div className="mb-4 flex justify-between text-sm"><span className="text-white/55">{label}</span><span>{formatVnd(value)}</span></div>; }

const declarationFieldLabels: Record<string, string> = {
  outputVat: "Thuế GTGT đầu ra", deductibleInputVat: "Thuế GTGT đầu vào được khấu trừ", payableVat: "Thuế GTGT phải nộp",
  employees: "Số lao động", taxableIncome: "Thu nhập chịu thuế", withheldTax: "Thuế đã khấu trừ",
  revenue: "Tổng doanh thu", deductibleExpenses: "Chi phí được trừ", payableTax: "Thuế TNDN phải nộp"
};

function Declarations({ data, refresh }: { data: TaxDatabase; refresh: () => Promise<void> }) {
  const [selectedId, setSelectedId] = useState(data.declarations[0]?.id ?? "");
  const selected = data.declarations.find(item => item.id === selectedId) ?? data.declarations[0];
  const [draft, setDraft] = useState<TaxDeclaration | undefined>(selected);
  const [message, setMessage] = useState("");
  useEffect(() => { setDraft(selected); setMessage(""); }, [selected]);
  if (!draft) return null;
  const invalid = Object.values(draft.values).some(value => typeof value === "number" && (!Number.isSafeInteger(value) || value < 0));
  async function save(lock = false) { if (!draft || invalid || draft.lockedAt) return; const next = { ...draft, status: lock ? "locked" as const : "valid" as const, updatedAt: new Date().toISOString(), lockedAt: lock ? new Date().toISOString() : undefined }; await taxApi.saveDeclaration(next); await refresh(); setMessage(lock ? "Đã khóa tờ khai." : "Đã lưu và kiểm tra dữ liệu."); }
  return <><Header eyebrow="Doanh nghiệp" title="Lập tờ khai thuế" description="Bộ hồ sơ GTGT, khấu trừ TNCN và quyết toán TNDN được quản lý độc lập với thuế hộ kinh doanh."/>{data.profile.taxpayerType !== "enterprise" && <div className="mb-5 rounded-2xl bg-blue-50 p-4 text-sm text-blue-700">Hãy chuyển “Đối tượng nộp thuế” sang Doanh nghiệp / công ty trong Hồ sơ để sử dụng bộ tờ khai này.</div>}<div className="grid gap-5 lg:grid-cols-[.8fr_1.5fr]"><section className="card space-y-3">{data.declarations.map(item => <button key={item.id} onClick={() => setSelectedId(item.id)} className={`w-full rounded-xl border p-4 text-left ${item.id === selectedId ? "border-pine bg-mint/50" : "hover:border-pine"}`}><div className="flex justify-between"><span className="font-semibold text-ink">{item.formCode}</span><span className={`badge ${item.status === "locked" ? "bg-slate-100" : item.status === "valid" ? "bg-emerald-50 text-emerald-700" : "bg-amber-50 text-amber-700"}`}>{item.status === "locked" ? "Đã khóa" : item.status === "valid" ? "Hợp lệ" : "Bản nháp"}</span></div><p className="mt-2 text-sm text-slate-500">{item.formName}</p><p className="mt-2 text-xs text-slate-400">{item.periodLabel}</p></button>)}</section><form onSubmit={(e) => { e.preventDefault(); void save(); }} className="card"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold text-pine">{draft.schemaVersion}</p><h2 className="mt-1 text-xl font-semibold text-ink">{draft.formCode} · {draft.periodLabel}</h2></div><span className="badge bg-slate-100 text-slate-600">{draft.periodType === "year" ? "Năm" : draft.periodType === "quarter" ? "Quý" : "Tháng"}</span></div><div className="mt-7 grid gap-5 md:grid-cols-2">{Object.entries(draft.values).map(([key, value]) => <Field key={key} label={declarationFieldLabels[key] ?? key}><input disabled={Boolean(draft.lockedAt)} className="field disabled:bg-slate-50" inputMode="numeric" value={String(value)} onChange={(e) => setDraft({...draft, values: {...draft.values, [key]: parseVnd(e.target.value)}})}/></Field>)}</div>{invalid && <p className="mt-5 text-sm text-red-600">Chỉ tiêu tiền phải là số nguyên VND không âm.</p>}{message && <p className="mt-5 text-sm text-pine">{message}</p>}<div className="mt-7 flex flex-wrap gap-3"><button disabled={Boolean(draft.lockedAt)} className="primary disabled:opacity-40">Lưu & kiểm tra</button><button type="button" disabled={Boolean(draft.lockedAt) || invalid} onClick={() => void save(true)} className="secondary disabled:opacity-40">Khóa tờ khai</button><button type="button" disabled className="secondary opacity-40" title="Chờ schema chính thức">Kết xuất XML</button></div><p className="mt-5 text-xs leading-5 text-slate-400">Chưa tạo XML/mã vạch. Các chức năng này chỉ được bật sau khi schema và quy tắc kiểm tra được xác nhận bằng HTKK/iTaxViewer.</p></form></div></>;
}

const accountingBooks = {
  non_taxable: [{ code: "S1a-HKD", name: "Sổ doanh thu bán hàng hóa, dịch vụ" }],
  revenue_percentage: [{ code: "S2a-HKD", name: "Sổ doanh thu theo nhóm ngành tính thuế" }],
  taxable_income: [
    { code: "S2b-HKD", name: "Sổ doanh thu bán hàng hóa, dịch vụ" },
    { code: "S2c-HKD", name: "Sổ chi tiết doanh thu, chi phí" },
    { code: "S2d-HKD", name: "Sổ vật liệu, dụng cụ, sản phẩm, hàng hóa" },
    { code: "S2e-HKD", name: "Sổ chi tiết tiền" }
  ]
} as const;

function Books({ data, go }: { data: TaxDatabase; go: (view: View) => void }) {
  const method = data.profile.householdTaxMethod ?? "revenue_percentage";
  const available = accountingBooks[method];
  const [selectedCode, setSelectedCode] = useState<string>(available[0].code);
  const selected = available.find(item => item.code === selectedCode) ?? available[0];
  const sorted = [...data.transactions].sort((a, b) => a.date.localeCompare(b.date));
  const revenue = sorted.filter(item => item.type === "revenue").reduce((sum, item) => sum + item.amount, 0);
  const expenses = sorted.filter(item => item.type === "expense").reduce((sum, item) => sum + item.amount, 0);
  const receivables = sorted.reduce((sum, item) => sum + (item.outstandingAmount ?? 0), 0);
  let cashBalance = 0;
  return <><Header eyebrow="Kế toán đơn giản" title="Sổ sách, tiền & công nợ" description="Sổ được chọn theo phương pháp thuế trong Hồ sơ, dữ liệu tự tổng hợp từ giao dịch và chứng từ."/><div className="grid gap-4 md:grid-cols-3"><Metric label="Dòng tiền thuần" value={formatVnd(revenue - expenses)} note={`${formatVnd(revenue)} thu · ${formatVnd(expenses)} chi`} accent="bg-pine"/><Metric label="Công nợ phải thu" value={formatVnd(receivables)} note={`${sorted.filter(item => (item.outstandingAmount ?? 0) > 0).length} hóa đơn chưa thu đủ`} accent="bg-coral"/><Metric label="Tồn kho" value="Chưa có dữ liệu" note="Nhập hàng hóa từ Excel hoặc hóa đơn" accent="bg-amber-400"/></div><div className="mt-6 grid gap-5 lg:grid-cols-[280px_1fr]"><section className="card h-fit"><p className="text-xs font-bold uppercase tracking-wider text-pine">Thông tư 152/2025/TT-BTC</p><div className="mt-4 space-y-2">{available.map(book => <button key={book.code} onClick={() => setSelectedCode(book.code)} className={`w-full rounded-xl border p-3 text-left ${selected.code === book.code ? "border-pine bg-mint/60" : "hover:border-pine"}`}><p className="font-semibold text-ink">{book.code}</p><p className="mt-1 text-xs leading-5 text-slate-500">{book.name}</p></button>)}</div><button onClick={() => go("profile")} className="link-button mt-5">Đổi phương pháp thuế →</button></section><section className="card min-w-0"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="section-title">{selected.code}</h2><p className="section-subtitle">{selected.name}</p></div><button onClick={() => go("history")} className="secondary">Xuất Excel</button></div>{selected.code === "S2d-HKD" ? <div className="mt-6 grid min-h-56 place-items-center rounded-2xl border border-dashed bg-slate-50 p-6 text-center"><div><p className="font-semibold text-ink">Chưa có dữ liệu tồn kho</p><p className="mt-2 text-sm text-slate-500">Bổ sung mã hàng, số lượng và đơn giá qua file nhập liệu.</p><button onClick={() => go("import")} className="primary mt-4">Nhập dữ liệu</button></div></div> : <div className="mt-5 overflow-x-auto"><table><thead><tr><th>Ngày</th><th>Chứng từ</th><th>Nội dung</th>{selected.code !== "S2e-HKD" && <th>Nhóm ngành</th>}<th className="text-right">Thu</th><th className="text-right">Chi</th>{selected.code === "S2e-HKD" && <th className="text-right">Tồn tiền</th>}</tr></thead><tbody>{sorted.filter(item => selected.code === "S2c-HKD" || selected.code === "S2e-HKD" ? true : item.type === "revenue").map(item => { cashBalance += item.type === "revenue" ? item.amount : -item.amount; return <tr key={item.id}><td>{new Date(item.date).toLocaleDateString("vi-VN")}</td><td>{item.documentNo || item.invoiceNo}</td><td className="font-medium text-ink">{item.description}</td>{selected.code !== "S2e-HKD" && <td>{item.revenueCategory}</td>}<td className="text-right text-emerald-700">{item.type === "revenue" ? formatVnd(item.amount) : "—"}</td><td className="text-right text-coral">{item.type === "expense" ? formatVnd(item.amount) : "—"}</td>{selected.code === "S2e-HKD" && <td className="text-right font-semibold">{formatVnd(cashBalance)}</td>}</tr>})}</tbody></table></div>}</section></div></>;
}

function UserGuide({ go }: { go: (view: View) => void }) {
  const steps: { title: string; text: string; view: View; action: string }[] = [
    { title: "1. Kiểm tra hồ sơ", text: "Chọn hộ kinh doanh/doanh nghiệp, ngành nghề, phương pháp thuế và kỳ khai tháng hoặc quý.", view: "profile", action: "Mở Hồ sơ" },
    { title: "2. Chọn kỳ kê khai", text: "Mỗi kỳ bắt đầu ở trạng thái Chưa nhập. Kiểm tra hạn nộp trước khi ghi dữ liệu.", view: "periods", action: "Xem các kỳ" },
    { title: "3. Nhập giao dịch", text: "Nhập doanh thu, chi phí, hóa đơn/chứng từ thủ công hoặc import Excel. Sửa lỗi và xóa dòng nhập sai trước khi khóa.", view: "transactions", action: "Nhập giao dịch" },
    { title: "4. Kiểm tra thuế", text: "Đối chiếu doanh thu theo nhóm ngành, GTGT, thuế thu nhập, số đã nộp và còn phải nộp.", view: "result", action: "Xem kết quả" },
    { title: "5. Khóa kỳ và xuất sổ", text: "Chỉ khóa sau khi hết cảnh báo. Sau khi khóa không thể sửa; xuất Excel và sổ kế toán để lưu trữ.", view: "history", action: "Xuất dữ liệu" }
  ];
  return <><Header eyebrow="Trợ giúp" title="Sổ tay sử dụng nhanh" description="Hoàn tất một kỳ kê khai theo 5 bước, từ hồ sơ đến khóa kỳ và xuất sổ."/><div className="grid gap-4 md:grid-cols-2">{steps.map(step => <section key={step.title} className="card"><h2 className="section-title">{step.title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{step.text}</p><button onClick={() => go(step.view)} className="link-button mt-4">{step.action} →</button></section>)}</div><section className="card mt-5"><h2 className="section-title">Ý nghĩa trạng thái kỳ</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><StatusHelp label="Chưa nhập" text="Kỳ chưa có giao dịch" color="bg-amber-50 text-amber-700"/><StatusHelp label="Đang nhập" text="Đã có dữ liệu, chưa hoàn tất" color="bg-amber-50 text-amber-700"/><StatusHelp label="Chờ duyệt" text="Chỉ khi bạn chủ động gửi duyệt" color="bg-blue-50 text-blue-700"/><StatusHelp label="Đã khóa" text="Đã lưu snapshot, không được sửa" color="bg-emerald-50 text-emerald-700"/></div></section></>;
}

function StatusHelp({ label, text, color }: { label: string; text: string; color: string }) { return <div className="rounded-xl border p-4"><span className={`badge ${color}`}>{label}</span><p className="mt-2 text-xs text-slate-500">{text}</p></div>; }

function History({ data }: { data: TaxDatabase }) {
  async function exportExcel() {
    const ExcelJS = await import("exceljs"); const period = data.periods[0]; const [items, calculated] = await Promise.all([taxApi.exportData(period.id), taxApi.calculate(period.id)]); const result = period.taxSnapshot ?? calculated; const book = new ExcelJS.Workbook();
    const summary = book.addWorksheet("Tong hop thue"); summary.addRows([["Kỳ", period.label], ["Phiên bản công thức", result.formulaVersion], ["Doanh thu", result.revenue], ["Thuế GTGT", result.vat], ["Thuế thu nhập", result.pit], ["Đã nộp", result.paid], ["Còn phải nộp", result.remaining]]);
    addTransactionSheet(book.addWorksheet("Chi tiet doanh thu"), items.filter(i => i.type === "revenue"));
    addTransactionSheet(book.addWorksheet("Chi phi"), items.filter(i => i.type === "expense"));
    const debt = book.addWorksheet("Cong no"); debt.columns = [{ header: "Số hóa đơn", key: "invoiceNo", width: 18 }, { header: "Khách hàng/Nội dung", key: "description", width: 32 }, { header: "Tổng tiền", key: "amount", width: 18 }, { header: "Còn phải thu", key: "outstandingAmount", width: 18 }]; items.filter(i => (i.outstandingAmount ?? 0) > 0).forEach(i => debt.addRow(i));
    addAccountingBookSheets(book, data.profile.householdTaxMethod ?? "revenue_percentage", items);
    downloadBuffer(await book.xlsx.writeBuffer(), `bao-cao-${period.id}.xlsx`);
  }
  return <><Header eyebrow="Đối soát" title="Lịch sử & xuất dữ liệu" description="Xuất dữ liệu phục vụ kê khai để kiểm tra thủ công; ứng dụng không gửi lên Thuế điện tử."/><div className="grid gap-5 lg:grid-cols-[1fr_2fr]"><section className="card h-fit"><h2 className="section-title">Bộ báo cáo Excel</h2><p className="mt-2 text-sm leading-6 text-slate-500">Gồm tổng hợp thuế, doanh thu, chi phí, công nợ và đúng nhóm sổ Thông tư 152 đã chọn trong Hồ sơ.</p><button onClick={() => void exportExcel()} className="primary mt-6 w-full">Xuất bộ báo cáo</button></section><section className="card"><h2 className="section-title">Lịch sử chỉnh sửa</h2><div className="mt-6 space-y-6">{data.audit.map((entry) => <div key={entry.id} className="relative border-l-2 border-mint pl-6 before:absolute before:-left-[7px] before:top-0 before:h-3 before:w-3 before:rounded-full before:bg-pine"><p className="font-semibold text-ink">{entry.action}</p><p className="mt-1 text-sm text-slate-500">{entry.detail}</p><p className="mt-2 text-xs text-slate-400">{new Date(entry.at).toLocaleString("vi-VN")}</p></div>)}</div></section></div></>;
}

function addTransactionSheet(sheet: import("exceljs").Worksheet, items: Transaction[]) {
  sheet.columns = [{ header: "Ngày", key: "date", width: 14 }, { header: "Nhóm ngành", key: "revenueCategory", width: 18 }, { header: "Nội dung", key: "description", width: 32 }, { header: "Số hóa đơn", key: "invoiceNo", width: 18 }, { header: "Số chứng từ", key: "documentNo", width: 18 }, { header: "Số tiền VND", key: "amount", width: 18 }, { header: "Thuế GTGT VND", key: "vatAmount", width: 18 }];
  items.forEach(item => sheet.addRow(item));
}

function addAccountingBookSheets(book: import("exceljs").Workbook, method: NonNullable<BusinessProfile["householdTaxMethod"]>, items: Transaction[]) {
  for (const definition of accountingBooks[method]) {
    const sheet = book.addWorksheet(definition.code);
    sheet.addRow([`${definition.name.toUpperCase()} (${definition.code})`]); sheet.mergeCells("A1:G1");
    if (definition.code === "S2d-HKD") {
      sheet.addRow(["Ngày", "Chứng từ", "Tên hàng hóa", "Đơn vị", "Nhập", "Xuất", "Tồn"]);
      continue;
    }
    sheet.addRow(["Ngày", "Hóa đơn/chứng từ", "Nội dung", "Nhóm ngành", "Thu", "Chi", "Ghi chú"]);
    const rows = definition.code === "S2c-HKD" || definition.code === "S2e-HKD" ? items : items.filter(item => item.type === "revenue");
    rows.forEach(item => sheet.addRow([item.date, item.documentNo || item.invoiceNo, item.description, item.revenueCategory, item.type === "revenue" ? item.amount : 0, item.type === "expense" ? item.amount : 0, ""]));
  }
}

function AccountLedgerField({ label, value, accounts, onChange }: { label: "TK Nợ" | "TK Có"; value: string; accounts: import("@/domain/tax").Account[]; onChange: (value: string) => void }) {
  const [open, setOpen] = useState(false);
  const matches = accounts.filter((item) => item.code.includes(value.trim()) || item.name.toLowerCase().includes(value.trim().toLowerCase())).slice(0, 12);
  const name = accounts.find((item) => item.code === value)?.name ?? "";
  return <div className="grid gap-3 md:grid-cols-[68px_120px_1fr]"><span className="self-center text-sm font-medium text-ink">{label} *</span><div className="relative"><input className="field" placeholder="Mã TK" value={value} onFocus={() => setOpen(true)} onChange={(event) => { onChange(event.target.value); setOpen(true); }} onBlur={() => window.setTimeout(() => setOpen(false), 150)}/>{open && matches.length > 0 && <div className="ledger-dropdown">{matches.map((item) => <button type="button" key={item.code} onMouseDown={() => { onChange(item.code); setOpen(false); }}><strong>{item.code}</strong><span>{item.name}</span></button>)}</div>}</div><input className="field bg-slate-50" aria-label={`Tên ${label}`} readOnly value={name} placeholder="Tên tài khoản"/></div>;
}

function CashReceiptTable({ receipts, accounts, onEdit }: { receipts: Transaction[]; accounts: import("@/domain/tax").Account[]; onEdit: (item: Transaction) => void }) {
  const accountName = (code?: string) => accounts.find((item) => item.code === code)?.name ?? "";
  const number = (value: number) => new Intl.NumberFormat("vi-VN", { maximumFractionDigits: 2 }).format(value);
  const rows = receipts.flatMap((item) => (item.cashReceipt?.entries ?? []).map((entry, index) => ({ item, entry, index })));
  return <section className="card mt-5"><h2 className="section-title">Phiếu thu đã lập</h2><p className="section-subtitle">Mỗi định khoản được hiển thị thành một dòng riêng.</p><div className="mt-4 overflow-x-auto"><table className="min-w-[1200px]"><thead><tr><th>Ngày</th><th>Số phiếu</th><th>Loại dòng</th><th>Diễn giải</th><th>TK Nợ</th><th>Chi tiết TK Nợ</th><th>TK Có</th><th>Chi tiết TK Có</th><th className="text-right">Số tiền VND</th><th>Hóa đơn</th></tr></thead><tbody>{rows.length === 0 ? <tr><td colSpan={10} className="py-8 text-center text-slate-400">Chưa có phiếu thu đã lập</td></tr> : rows.map(({ item, entry, index }) => { const detail = item.cashReceipt!; return <tr key={`${item.id}-${entry.id ?? index}`} className="cursor-pointer hover:bg-mint/30" onClick={() => onEdit(item)}><td>{detail.voucherDate}</td><td className="font-medium text-ink">{item.documentNo || item.invoiceNo}</td><td><span className={`badge ${entry.kind === "vat" ? "bg-amber-50 text-amber-700" : "bg-emerald-50 text-emerald-700"}`}>{entry.kind === "vat" ? `Thuế GTGT ${entry.rate ?? 0}%` : "Doanh thu"}</span></td><td>{entry.description}</td><td>{entry.debitAccount}</td><td>{accountName(entry.debitAccount)}</td><td>{entry.creditAccount}</td><td>{accountName(entry.creditAccount)}</td><td className="text-right font-semibold">{number(entry.amount)}</td><td>{detail.invoiceNo}</td></tr>; })}</tbody></table></div></section>;
}

function suggestReceiptNo(receipts: Transaction[]) {
  const mm = String(new Date().getMonth() + 1).padStart(2, "0");
  const numbers = receipts.map(item => Number((item.documentNo || item.invoiceNo || "").match(/^(\d+)\//)?.[1] ?? 0)).filter(n => n > 0);
  return `${String((numbers.length ? Math.max(...numbers) : 0) + 1).padStart(2, "0")}/${mm}`;
}

function CashReceiptDetail({ data, refresh, onEndSession }: { data: TaxDatabase; refresh: () => Promise<void>; onEndSession: () => void }) {
  const blank = (): CashReceipt => ({ periodId: data.periods[0].id, voucherDate: currentDateInputValue(), accountingDate: currentDateInputValue(), status: "draft", receiptNo: "", counterpartyCode: "", counterpartyName: "", counterpartyTaxCode: "", counterpartyAddress: "", contactName: "", description: "", debitAccount: "1111", creditAccount: "511", amount: 0, amountIncludesVAT: false, currency: "VND", exchangeRate: 1, convertedAmount: 0, revenueCategory: data.profile.industry, invoiceNo: "", invoiceSymbol: "", invoiceDate: "", detailCode: "DOANH_THU", quantity: "1", unitPrice: 0, caseCode: "", collector: "", note: "", attachments: [], entries: [], invoices: [], taxLines: [], saveCounterparty: false });
  const [form, setForm] = useState<CashReceipt>(blank); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false);
  const [sessionBaseAmount, setSessionBaseAmount] = useState(0); const [sessionLines, setSessionLines] = useState<CashReceipt[]>([]); const [moreOpen, setMoreOpen] = useState(false);
  const invoiceNoRef = useRef<HTMLInputElement>(null);
  const receipts = data.transactions.filter((item) => item.voucherType === "cash_receipt");
  const [accounts, setAccounts] = useState(data.accounts ?? []);
  useEffect(() => { let active = true; void taxApi.getAccounts().then((items) => { if (active) setAccounts(items); }).catch(() => { if (active) setMessage("Không tải được danh mục tài khoản."); }); return () => { active = false; }; }, []);
  const set = <K extends keyof CashReceipt>(key: K, value: CashReceipt[K]) => setForm((current) => ({ ...current, [key]: value }));
  async function lookup() { if (!form.counterpartyCode.trim()) return; try { const item = await taxApi.getCounterparty(form.counterpartyCode.trim()); setForm((current) => ({ ...current, counterpartyName: item.name, counterpartyTaxCode: item.taxCode, counterpartyAddress: item.address })); } catch { setMessage("Không tìm thấy mã đối tượng; bạn có thể nhập thông tin và chọn lưu danh mục."); } }
  function requestVATRate(baseAmount: number) {
    const raw = window.prompt("Nhập % thuế GTGT (0, 5, 8 hoặc 10)", "8");
    if (raw === null) return null;
    const rate = Number(raw.replace(",", "."));
    if (![0, 5, 8, 10].includes(rate)) { setMessage("Thuế suất GTGT chỉ nhận 0%, 5%, 8% hoặc 10%."); return null; }
    return { rate, amount: Math.round(baseAmount * rate / 100) };
  }
  function selectCreditAccount(value: string) {
    if (value !== "33311") { set("creditAccount", value); return; }
    const baseAmount = sessionBaseAmount || sessionLines.find(line => !/^333/.test(line.creditAccount))?.amount || form.amount;
    const vat = requestVATRate(baseAmount);
    if (!vat) return;
    setForm(current => ({ ...current, creditAccount: "33311", amount: vat.amount, description: `Thuế GTGT đầu ra ${vat.rate}%` }));
  }
  function validLine(line: CashReceipt) { const validAmount = /^333/.test(line.creditAccount) ? line.amount >= 0 : line.amount > 0; return Boolean(line.receiptNo.trim() && line.counterpartyName.trim() && line.description.trim() && line.invoiceNo.trim() && validAmount && isValidDateInput(line.voucherDate) && isValidDateInput(line.accountingDate) && /^\d{3,10}$/.test(line.debitAccount) && /^\d{3,10}$/.test(line.creditAccount)); }
  async function save(status: "draft" | "saved") {
    const lines = [...sessionLines, ...(validLine(form) && !sessionLines.includes(form) ? [form] : [])];
    if (lines.length === 0 || lines.some(line => !validLine(line))) { setMessage("Kiểm tra các trường bắt buộc (*), hóa đơn, số tiền và tài khoản Nợ/Có."); return false; }
    const normalIndex = lines.findIndex(line => !/^333/.test(line.creditAccount)); if (normalIndex < 0) { setMessage("Phiếu thu phải có một dòng định khoản doanh thu trước dòng thuế."); return false; }
    const invoiceID = crypto.randomUUID(); const revenueID = crypto.randomUUID();
    const entries: import("@/domain/tax").ReceiptAccountingEntry[] = lines.map((line, index) => ({ id: index === normalIndex ? revenueID : crypto.randomUUID(), invoiceId: invoiceID, debitAccount: line.debitAccount, creditAccount: line.creditAccount, amount: line.amount, description: line.description, kind: /^333/.test(line.creditAccount) ? "vat" : "normal", rate: /^333/.test(line.creditAccount) ? Math.round(line.amount / lines[normalIndex].amount * 100) : undefined, detailCode: index === normalIndex ? (line.detailCode || "DOANH_THU") : undefined, quantity: index === normalIndex ? (line.quantity || "1") : undefined, unitPrice: index === normalIndex ? line.amount : undefined, revenueDetailId: /^333/.test(line.creditAccount) ? revenueID : undefined }));
    const total = entries.reduce((sum, entry) => sum + entry.amount, 0); const first = lines[0];
    const payload: CashReceipt = { ...first, id: form.id, status, amount: total, convertedAmount: Math.round(total * first.exchangeRate), entries, invoices: [{ id: invoiceID, invoiceNo: first.invoiceNo, symbol: first.invoiceSymbol ?? "", invoiceDate: first.invoiceDate, taxCode: first.counterpartyTaxCode }], taxLines: [] };
    try { setBusy(true); if (payload.id) await taxApi.updateCashReceipt(payload as CashReceipt & { id: string }); else await taxApi.createCashReceipt(payload); await refresh(); setMessage(status === "draft" ? "Đã lưu nháp phiếu thu." : "Đã lưu phiếu thu."); return true; } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Không thể lưu phiếu thu"); return false; } finally { setBusy(false); }
  }
  function edit(item: Transaction, clone = false) { const d = item.cashReceipt; if (!d) return; const header = { ...blank(), ...d, id: clone ? undefined : item.id, periodId: item.periodId, receiptNo: clone ? `${item.documentNo || item.invoiceNo}-COPY` : item.documentNo || item.invoiceNo, counterpartyCode: item.counterpartyCode ?? "", counterpartyName: item.counterpartyName ?? "", counterpartyTaxCode: item.counterpartyTaxCode ?? "", counterpartyAddress: item.counterpartyAddress ?? "", revenueCategory: item.revenueCategory, attachments: d.attachments ?? [], saveCounterparty: false }; const savedEntries = d.entries ?? []; setSessionLines(savedEntries.map(entry => ({ ...header, description: entry.description, debitAccount: entry.debitAccount, creditAccount: entry.creditAccount, amount: entry.amount }))); setForm({ ...header, amount: 0, description: "" }); setSessionBaseAmount(savedEntries.find(entry => entry.kind === "normal")?.amount ?? 0); }
  async function remove() { if (!form.id || !window.confirm("Xóa phiếu thu này?")) return; await taxApi.deleteTransaction(form.id); setForm(blank()); await refresh(); }
  function print() { window.print(); }
  async function addLine() { if (!validLine(form)) { setMessage("Kiểm tra các trường bắt buộc (*), hóa đơn, số tiền và tài khoản Nợ/Có."); return; } const base = form.amount; setSessionLines(current => [...current, form]); if (/^333/.test(form.creditAccount)) { setForm(current => ({ ...current, amount: 0, description: "", creditAccount: "511" })); setMessage("Đã thêm dòng thuế vào phiên nhập; dữ liệu chưa được lưu."); return; } setSessionBaseAmount(base); const vat = requestVATRate(base); setForm(current => ({ ...current, id: undefined, creditAccount: vat ? "33311" : "", amount: vat?.amount ?? 0, description: vat ? `Thuế GTGT đầu ra ${vat.rate}%` : "Thuế GTGT đầu ra" })); setMessage(vat ? `Đã thêm doanh thu ${formatVnd(base)}; thuế GTGT ${vat.rate}% là ${formatVnd(vat.amount)}. Bấm Kết thúc để lưu.` : "Đã thêm định khoản doanh thu; chọn TK 33311 để nhập % thuế GTGT."); }
  function newVoucher() { setForm({ ...blank(), receiptNo: suggestReceiptNo(receipts) }); setSessionLines([]); setSessionBaseAmount(0); setMessage("Đã mở phiên nhập chứng từ mới; các dòng chỉ được lưu khi bấm Kết thúc."); }
  async function endSession() { if (!await save("saved")) return; setForm(blank()); setSessionLines([]); setSessionBaseAmount(0); onEndSession(); }
  return <><Header eyebrow="Chứng từ" title="Phiếu thu tiền mặt" description="Thêm chứng từ để mở phiên nhập, thêm các định khoản doanh thu/thuế và chỉ lưu toàn bộ khi bấm Kết thúc."/>{message && <p className="mb-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">{message}</p>}<CashReceiptTable receipts={receipts} accounts={accounts} onEdit={item => edit(item)}/>{sessionLines.length > 0 && <section className="card mt-5 overflow-x-auto"><h2 className="section-title">Định khoản đang nhập (chưa lưu)</h2><table className="mt-4"><thead><tr><th>Diễn giải</th><th>TK Nợ</th><th>TK Có</th><th className="text-right">Số tiền</th><th></th></tr></thead><tbody>{sessionLines.map((line, index) => <tr key={`${line.creditAccount}-${index}`}><td>{line.description}</td><td>{line.debitAccount}</td><td>{line.creditAccount}</td><td className="text-right">{formatVnd(line.amount)}</td><td><button type="button" className="text-xs text-red-600" onClick={() => setSessionLines(current => current.filter((_, itemIndex) => itemIndex !== index))}>Xóa dòng</button></td></tr>)}</tbody></table></section>}<form onSubmit={(e) => { e.preventDefault(); void addLine(); }} className="card mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Field label="Số phiếu thu *"><input className="field" value={form.receiptNo} onChange={e => set("receiptNo", e.target.value)}/></Field><Field label="Ngày chứng từ *"><input type="date" className="field" value={form.voucherDate} onChange={e => set("voucherDate", e.target.value)}/></Field><Field label="Ngày hạch toán *"><input type="date" className="field" value={form.accountingDate} onChange={e => set("accountingDate", e.target.value)}/></Field><Field label="Trạng thái"><input className="field" disabled value={form.status === "draft" ? "Nháp" : "Đã lưu"}/></Field><Field label="Mã đối tượng"><input list="cash-counterparties" className="field" value={form.counterpartyCode} onChange={e => set("counterpartyCode", e.target.value)} onBlur={() => void lookup()}/><datalist id="cash-counterparties">{(data.counterparties ?? []).map(item => <option key={item.code} value={item.code}/>)}</datalist></Field><Field label="Họ tên/đơn vị *"><input className="field" value={form.counterpartyName} onChange={e => set("counterpartyName", e.target.value)}/></Field><Field label="Mã số thuế"><input className="field" value={form.counterpartyTaxCode} onChange={e => set("counterpartyTaxCode", e.target.value)}/></Field><Field label="Người liên hệ"><input className="field" value={form.contactName} onChange={e => set("contactName", e.target.value)}/></Field><Field label="Địa chỉ" wide><input className="field" value={form.counterpartyAddress} onChange={e => set("counterpartyAddress", e.target.value)}/></Field><Field label="Lý do thu *" wide><input className="field" value={form.description} onChange={e => set("description", e.target.value)}/></Field><div className="md:col-span-2 lg:col-span-4 grid gap-4 md:grid-cols-2"><AccountLedgerField label="TK Nợ" value={form.debitAccount} accounts={accounts} onChange={value => set("debitAccount", value)}/><div><AccountLedgerField label="TK Có" value={form.creditAccount} accounts={accounts} onChange={selectCreditAccount}/>{/^333/.test(form.creditAccount) && <p className="mt-1 text-xs text-slate-400">Dòng thuế GTGT đầu ra — số tiền đã tính sẵn 8%, có thể sửa.</p>}</div></div><Field label="Số tiền *"><input className="field" inputMode="numeric" value={form.amount || ""} onChange={e => set("amount", parseVnd(e.target.value))}/></Field><Field label="Loại tiền *"><select className="field" value={form.currency} onChange={e => set("currency", e.target.value)}><option>VND</option><option>USD</option><option>EUR</option></select></Field><Field label="Tỷ giá"><input className="field" inputMode="decimal" value={form.exchangeRate} onChange={e => set("exchangeRate", Number(e.target.value))}/></Field><Field label="Số tiền quy đổi"><input className="field" disabled value={formatVnd(Math.round(form.amount * form.exchangeRate))}/></Field><Field label="Số hóa đơn"><input ref={invoiceNoRef} className="field" value={form.invoiceNo} onChange={e => set("invoiceNo", e.target.value)}/></Field><Field label="Ngày hóa đơn"><input type="date" className="field" value={form.invoiceDate} onChange={e => set("invoiceDate", e.target.value)}/></Field><Field label="Mã vụ việc"><input className="field" value={form.caseCode} onChange={e => set("caseCode", e.target.value)}/></Field><Field label="Nhân viên thu"><input className="field" value={form.collector} onChange={e => set("collector", e.target.value)}/></Field><Field label="Ghi chú" wide><textarea className="field min-h-20" value={form.note} onChange={e => set("note", e.target.value)}/></Field><Field label="File chứng từ đính kèm" wide><input type="file" multiple onChange={e => set("attachments", Array.from(e.target.files ?? []).map(file => ({ name: file.name, type: file.type, size: file.size, data: "" })))}/></Field><label className="text-sm"><input type="checkbox" checked={form.saveCounterparty} onChange={e => set("saveCounterparty", e.target.checked)}/> Lưu/cập nhật vào danh mục đối tượng</label><div className="flex flex-wrap items-end gap-2 lg:col-span-4"><button type="button" onClick={() => void addLine()} disabled={busy} className="secondary">F2 · Thêm định khoản</button><button type="button" onClick={() => invoiceNoRef.current?.focus()} className="secondary">F3 · Thêm hóa đơn</button><button type="button" onClick={newVoucher} className="secondary">F4 · Thêm chứng từ</button><button type="button" onClick={() => void addLine()} disabled={busy} className="primary">F5 · Cập nhật dòng</button><button type="button" onClick={remove} disabled={!form.id} className="secondary">F6 · Xóa</button><div className="relative"><button type="button" onClick={() => setMoreOpen(open => !open)} className="secondary">F7 · Các lệnh khác</button>{moreOpen && <div className="ledger-dropdown"><button type="button" onMouseDown={() => { setMoreOpen(false); print(); }}>In phiếu thu</button></div>}</div><button type="button" disabled={busy} onClick={() => void endSession()} className="primary">Kết thúc & lưu</button></div></form></>;
}

function CashPaymentTable({ payments, accounts, onEdit }: { payments: Transaction[]; accounts: import("@/domain/tax").Account[]; onEdit: (item: Transaction) => void }) {
  const accountName = (code?: string) => accounts.find((item) => item.code === code)?.name ?? "";
  const number = (value: number) => new Intl.NumberFormat("vi-VN", { maximumFractionDigits: 2 }).format(value);
  return <section className="card mt-5"><h2 className="section-title">Phiếu chi đã lập</h2><p className="section-subtitle">Bấm vào một dòng để mở lại phiếu chi.</p><div className="mt-4 overflow-x-auto"><table className="min-w-[1600px]"><thead><tr><th>Ngày</th><th>CTGS</th><th>Số phiếu chi</th><th>Ngày phiếu</th><th>Diễn giải</th><th>TK Nợ</th><th>Chi tiết TK Nợ</th><th>TK Có</th><th>Chi tiết TK Có</th><th className="text-right">Số lượng</th><th className="text-right">Đơn giá</th><th className="text-right">Số tiền VND</th><th className="text-right">Nguyên tệ</th><th className="text-right">Tỷ giá</th><th>Hóa đơn</th><th>KKBS</th></tr></thead><tbody>{payments.map((item) => { const detail = item.cashPayment; if (!detail) return null; return <tr key={item.id} className="cursor-pointer hover:bg-mint/30" onClick={() => onEdit(item)}><td>{detail.postingDate}</td><td className="font-medium text-ink">{detail.ctgsNo}</td><td className="font-medium text-ink">{item.documentNo || item.invoiceNo}</td><td>{detail.voucherDate}</td><td>{item.description}</td><td>{detail.debitAccount}</td><td>{accountName(detail.debitAccount)}</td><td>{detail.creditAccount}</td><td>{accountName(detail.creditAccount)}</td><td className="text-right">{number(detail.quantity)}</td><td className="text-right">{number(detail.unitPrice)}</td><td className="text-right font-semibold">{number(item.amount)}</td><td className="text-right">{detail.currency === "VND" ? "—" : detail.currency}</td><td className="text-right">{number(detail.exchangeRate)}</td><td>{detail.invoiceNo}</td><td>—</td></tr>; })}</tbody></table></div></section>;
}

function CashPaymentDetail({ data, refresh, onEndSession }: { data: TaxDatabase; refresh: () => Promise<void>; onEndSession: () => void }) {
  const blank = (): CashPayment => ({ periodId: data.periods[0].id, status: "draft", month: currentMonthLabel(), ctgsNo: "", postingDate: currentDateInputValue(), voucherDate: currentDateInputValue(), paymentNo: "", recipientCode: "", recipientName: "", recipientTaxCode: "", recipientAddress: "", relatedDocumentNo: "", description: "", invoiceType: "Hóa đơn GTGT", invoiceNo: "", invoiceSerial: "", invoiceDate: "", unitName: "", unitAddress: "", itemName: "", warehouseCode: "", debitAccount: "", debitSub1: "", debitSub2: "", creditAccount: "1111", creditSub1: "", creditSub2: "", caseCode: "", quantity: 1, unitPrice: 0, currency: "VND", exchangeRate: 1, amount: 0, endingStock: "", note: "", attachments: [], savePayee: false });
  const [form, setForm] = useState<CashPayment>(blank); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); const [moreOpen, setMoreOpen] = useState(false);
  const invoiceNoRef = useRef<HTMLInputElement>(null); const fileInputRef = useRef<HTMLInputElement>(null);
  const payments = data.transactions.filter((item) => item.voucherType === "cash_payment");
  const [accounts, setAccounts] = useState(data.accounts ?? []);
  useEffect(() => { let active = true; void taxApi.getAccounts().then((items) => { if (active) setAccounts(items); }).catch(() => { if (active) setMessage("Không tải được danh mục tài khoản."); }); return () => { active = false; }; }, []);
  const set = <K extends keyof CashPayment>(key: K, value: CashPayment[K]) => setForm((current) => ({ ...current, [key]: value }));
  async function lookup() { if (!form.recipientCode.trim()) return; try { const item = await taxApi.getCounterparty(form.recipientCode.trim()); setForm((current) => ({ ...current, recipientName: item.name, recipientTaxCode: item.taxCode, recipientAddress: item.address })); } catch { setMessage("Không tìm thấy mã đối tượng; bạn có thể nhập thông tin và chọn lưu danh mục."); } }
  const computedAmount = Math.round(form.quantity * form.unitPrice * form.exchangeRate);
  function accountBalance(code: string) {
    if (!code.trim()) return 0;
    return data.transactions.reduce((sum, item) => {
      const debit = item.cashReceipt?.debitAccount ?? item.cashPayment?.debitAccount; const credit = item.cashReceipt?.creditAccount ?? item.cashPayment?.creditAccount;
      const posted = item.cashReceipt?.status === "saved" || item.cashPayment?.status === "saved";
      if (!posted) return sum;
      return sum + (debit === code ? item.amount : 0) - (credit === code ? item.amount : 0);
    }, 0);
  }
  async function save(status: "draft" | "saved") {
    setMessage("");
    if (!form.paymentNo.trim() || !form.recipientName.trim() || !form.description.trim() || computedAmount <= 0 || !isValidDateInput(form.voucherDate) || !isValidDateInput(form.postingDate) || !/^\d{3,10}$/.test(form.debitAccount) || !/^\d{3,10}$/.test(form.creditAccount)) { setMessage("Kiểm tra các trường bắt buộc (*), số lượng/đơn giá và tài khoản Nợ/Có."); return; }
    try { setBusy(true); const payload = { ...form, status, amount: computedAmount }; if (form.id) await taxApi.updateCashPayment(payload as CashPayment & { id: string }); else await taxApi.createCashPayment(payload); await refresh(); setMessage(status === "draft" ? "Đã lưu nháp phiếu chi." : "Đã lưu phiếu chi."); } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Không thể lưu phiếu chi"); } finally { setBusy(false); }
  }
  function edit(item: Transaction, clone = false) { const d = item.cashPayment; if (!d) return; setForm({ ...blank(), ...d, id: clone ? undefined : item.id, periodId: item.periodId, paymentNo: clone ? `${item.documentNo || item.invoiceNo}-COPY` : item.documentNo || item.invoiceNo, recipientCode: item.counterpartyCode ?? "", recipientName: item.counterpartyName ?? "", recipientTaxCode: item.counterpartyTaxCode ?? "", recipientAddress: item.counterpartyAddress ?? "", description: item.description, amount: item.amount, savePayee: false }); }
  async function remove() { if (!form.id || !window.confirm("Xóa phiếu chi này?")) return; await taxApi.deleteTransaction(form.id); setForm(blank()); await refresh(); }
  function print() { window.print(); }
  return <><Header eyebrow="Chứng từ" title="Phiếu chi tiền mặt" description="Lập và hạch toán phiếu chi; các trường có dấu * là bắt buộc. Danh sách bên dưới chỉ hiển thị trong phiên; bấm “Kết thúc” để đóng phiên."/>{message && <p className="mb-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">{message}</p>}
    {payments.length > 0 && <CashPaymentTable payments={payments} accounts={accounts} onEdit={item => edit(item)}/>}
    <form onSubmit={(e) => { e.preventDefault(); void save("saved"); }} className="card mt-5 grid gap-4">
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Field label="Loại chứng từ"><input className="field" disabled value="Phiếu chi tiền mặt"/></Field><Field label="Tháng"><input className="field" value={form.month} onChange={e => set("month", e.target.value)}/></Field><Field label="Số CTGS"><input className="field" value={form.ctgsNo} onChange={e => set("ctgsNo", e.target.value)}/></Field><Field label="Ngày ghi sổ *"><input type="date" className="field" value={form.postingDate} onChange={e => set("postingDate", e.target.value)}/></Field></div>
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Field label="Số phiếu chi *"><input className="field" value={form.paymentNo} onChange={e => set("paymentNo", e.target.value)}/></Field><Field label="Ngày phiếu *"><input type="date" className="field" value={form.voucherDate} onChange={e => set("voucherDate", e.target.value)}/></Field><Field label="Người nhận *"><input list="cash-payment-recipients" className="field" value={form.recipientCode} onChange={e => set("recipientCode", e.target.value)} onBlur={() => void lookup()} placeholder="Mã đối tượng"/><datalist id="cash-payment-recipients">{(data.counterparties ?? []).map(item => <option key={item.code} value={item.code}/>)}</datalist></Field><Field label="Tên người nhận *"><input className="field" value={form.recipientName} onChange={e => set("recipientName", e.target.value)}/></Field><Field label="Địa chỉ người nhận" wide><input className="field" value={form.recipientAddress} onChange={e => set("recipientAddress", e.target.value)}/></Field><Field label="Mã số thuế"><input className="field" value={form.recipientTaxCode} onChange={e => set("recipientTaxCode", e.target.value)}/></Field><Field label="Phiếu nhập liên quan"><input className="field" value={form.relatedDocumentNo} onChange={e => set("relatedDocumentNo", e.target.value)}/></Field><Field label="Diễn giải *" wide><input className="field" value={form.description} onChange={e => set("description", e.target.value)}/></Field><Field label="Chứng từ kèm theo" wide><input ref={fileInputRef} type="file" multiple onChange={e => set("attachments", Array.from(e.target.files ?? []).map(file => ({ name: file.name, type: file.type, size: file.size, data: "" })))}/></Field></div>
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Field label="Loại hóa đơn"><select className="field" value={form.invoiceType} onChange={e => set("invoiceType", e.target.value)}><option>Hóa đơn GTGT</option><option>Hóa đơn bán hàng</option><option>Hóa đơn khác</option></select></Field><Field label="Số hóa đơn"><input ref={invoiceNoRef} className="field" value={form.invoiceNo} onChange={e => set("invoiceNo", e.target.value)}/></Field><Field label="Số xê-ri"><input className="field" value={form.invoiceSerial} onChange={e => set("invoiceSerial", e.target.value)}/></Field><Field label="Ngày hóa đơn"><input type="date" className="field" value={form.invoiceDate} onChange={e => set("invoiceDate", e.target.value)}/></Field><Field label="Đơn vị"><input className="field" value={form.unitName} onChange={e => set("unitName", e.target.value)}/></Field><Field label="Địa chỉ đơn vị" wide><input className="field" value={form.unitAddress} onChange={e => set("unitAddress", e.target.value)}/></Field><Field label="Mặt hàng"><input className="field" value={form.itemName} onChange={e => set("itemName", e.target.value)}/></Field><Field label="Mã kho"><input className="field" value={form.warehouseCode} onChange={e => set("warehouseCode", e.target.value)}/></Field></div>
      <div className="grid gap-4 md:grid-cols-2"><div className="grid gap-2"><AccountLedgerField label="TK Nợ" value={form.debitAccount} accounts={accounts} onChange={value => set("debitAccount", value)}/><div className="grid gap-2 md:grid-cols-2"><input className="field" placeholder="Chi tiết cấp 1" value={form.debitSub1} onChange={e => set("debitSub1", e.target.value)}/><input className="field" placeholder="Chi tiết cấp 2" value={form.debitSub2} onChange={e => set("debitSub2", e.target.value)}/></div><p className="text-xs text-slate-500">Số dư cuối TK Nợ: {formatVnd(accountBalance(form.debitAccount))}</p></div><div className="grid gap-2"><AccountLedgerField label="TK Có" value={form.creditAccount} accounts={accounts} onChange={value => set("creditAccount", value)}/><div className="grid gap-2 md:grid-cols-2"><input className="field" placeholder="Chi tiết cấp 1" value={form.creditSub1} onChange={e => set("creditSub1", e.target.value)}/><input className="field" placeholder="Chi tiết cấp 2" value={form.creditSub2} onChange={e => set("creditSub2", e.target.value)}/></div><p className="text-xs text-slate-500">Số dư cuối TK Có: {formatVnd(accountBalance(form.creditAccount))}</p></div></div>
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Field label="Mã vụ việc"><input className="field" value={form.caseCode} onChange={e => set("caseCode", e.target.value)}/></Field><Field label="Số lượng *"><input className="field" inputMode="numeric" value={form.quantity || ""} onChange={e => set("quantity", Number(e.target.value.replace(/\D/g, "")) || 0)}/></Field><Field label="Đơn giá *"><input className="field" inputMode="numeric" value={form.unitPrice || ""} onChange={e => set("unitPrice", parseVnd(e.target.value))}/></Field><Field label="Nguyên tệ"><select className="field" value={form.currency} onChange={e => set("currency", e.target.value)}><option>VND</option><option>USD</option><option>EUR</option></select></Field><Field label="Tỷ giá"><input className="field" inputMode="decimal" value={form.exchangeRate} onChange={e => set("exchangeRate", Number(e.target.value) || 0)}/></Field><Field label="Số tiền"><input className="field" disabled value={formatVnd(computedAmount)}/></Field><Field label="Số tồn cuối"><input className="field" value={form.endingStock} onChange={e => set("endingStock", e.target.value)} placeholder="Nhập thủ công"/></Field><Field label="Ghi chú" wide><textarea className="field min-h-20" value={form.note} onChange={e => set("note", e.target.value)}/></Field></div>
      <label className="text-sm"><input type="checkbox" checked={form.savePayee} onChange={e => set("savePayee", e.target.checked)}/> Lưu/cập nhật vào danh mục đối tượng</label>
      <div className="flex flex-wrap items-end gap-2">
        <button type="button" onClick={() => { setForm(blank()); setMessage(""); }} className="secondary">F2 · Thêm định khoản</button>
        <button type="button" onClick={() => invoiceNoRef.current?.focus()} className="secondary">F3 · Thêm hóa đơn</button>
        <button type="button" onClick={() => { setForm(blank()); setMessage(""); }} className="secondary">F4 · Thêm chứng từ</button>
        <button disabled={busy} className="primary">F5 · Sửa</button>
        <button type="button" onClick={remove} disabled={!form.id} className="secondary">F6 · Xóa</button>
        <div className="relative"><button type="button" onClick={() => setMoreOpen(open => !open)} className="secondary">F7 · Các lệnh khác</button>{moreOpen && <div className="ledger-dropdown"><button type="button" onMouseDown={() => { setMoreOpen(false); void save("draft"); }}>Lưu nháp</button><button type="button" onMouseDown={() => { setMoreOpen(false); setForm(current => ({ ...current, id: undefined, paymentNo: `${current.paymentNo}-COPY` })); }}>Nhân bản</button><button type="button" onMouseDown={() => { setMoreOpen(false); print(); }}>In phiếu chi</button></div>}</div>
        <button type="button" onClick={() => { setForm(blank()); setMessage(""); onEndSession(); }} className="secondary">Kết thúc</button>
      </div>
    </form>
  </>;
}

const voucherTypeOptions: { value: "cash_receipt" | "cash_payment"; label: string }[] = [
  { value: "cash_receipt", label: "01. Phiếu thu tiền mặt" },
  { value: "cash_payment", label: "02. Phiếu chi tiền mặt" }
];
const voucherTypePlaceholders = [
  "03. Hàng mua chưa thanh toán", "04. Phiếu xuất kho nguyên vật liệu", "05. Phiếu chuyển kho",
  "06. Chứng từ doanh thu không thu tiền ngay", "07. Thu ngân hàng", "08. Chi ngân hàng", "09. Chứng từ khác",
  "10. Phiếu xuất kho giá vốn", "12. Ngân hàng - ACB", "13. Ngân hàng SACOMBANK", "14. Ngân hàng MB",
  "15. Phiếu xuất kho thành phẩm", "16. Phiếu nhập kho thành phẩm", "17. Chứng từ chi lương", "99. Chứng từ kết chuyển cuối kỳ"
];

function VoucherEntry({ data, refresh }: { data: TaxDatabase; refresh: () => Promise<void> }) {
  const [docType, setDocType] = useState<"" | "cash_receipt" | "cash_payment">("");
  function endSession() { setDocType(""); }
  return <>
    <section className="card mb-5">
      <Field label="Loại chứng từ" wide>
        <select className="field" value={docType} onChange={(e) => setDocType(e.target.value as "" | "cash_receipt" | "cash_payment")}>
          <option value="">→ Bạn hãy chọn loại chứng từ...</option>
          {voucherTypeOptions.map(item => <option key={item.value} value={item.value}>{item.label}</option>)}
          {voucherTypePlaceholders.map(label => <option key={label} disabled>{label} · Phát triển sau</option>)}
        </select>
      </Field>
      {!docType && <p className="mt-3 text-sm text-slate-500">Chọn loại chứng từ để bắt đầu.</p>}
    </section>
    {docType === "cash_receipt" && <CashReceiptDetail data={data} refresh={refresh} onEndSession={endSession}/>}
    {docType === "cash_payment" && <CashPaymentDetail data={data} refresh={refresh} onEndSession={endSession}/>}
  </>;
}

export function ViewContent({ view, data, activePeriodId, selectPeriod, refresh, go }: { view: View; data: TaxDatabase; activePeriodId: string; selectPeriod: (periodId: string) => void; refresh: () => Promise<void>; go: (view: View) => void }) {
  if (view === "profile") return <Profile data={data} refresh={refresh}/>;
  if (view === "periods") return <><PeriodPicker data={data} activePeriodId={activePeriodId} selectPeriod={selectPeriod} go={go}/><Periods data={data} go={go}/></>;
  if (view === "transactions") return <VoucherEntry data={data} refresh={refresh}/>;
  if (view === "books") return <Books data={data} go={go}/>;
  if (view === "import") return <><ImportExcel data={data} refresh={refresh}/><ElectronicInvoiceImport/></>;
  if (view === "result") return data.profile.taxpayerType === "enterprise" ? <Declarations data={data} refresh={refresh}/> : <Result data={data} refresh={refresh}/>;
  if (view === "declarations") return <Declarations data={data} refresh={refresh}/>;
  if (view === "history") return <History data={data}/>;
  if (view === "guide") return <UserGuide go={go}/>;
  return <Dashboard data={data} go={go}/>;
}

function downloadBuffer(buffer: ArrayBuffer, filename: string) {
  const url = URL.createObjectURL(new Blob([buffer], { type: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" }));
  const link = document.createElement("a"); link.href = url; link.download = filename; link.click(); URL.revokeObjectURL(url);
}
